cd backend
npm init -y
npm install express mongoose cors dotenv

npm install nodemon —save -dev (package.json - “dev”: “nodemon index.js”)
npm install —save -dev nodemon

.env file
PORT=5000
MONGO_URI=mongodb://connection string

.gitignore file
node_modules
.env

Git connect 
git init
git add .
git commit -m “commit message”
git remote add origin //link
git branch -M main
git push -u origin main

Test
Backend -> npm run dev -> MongoDB connected message 
Server running on port 5000

Frontend -> npm run dev -> open link

Add new field 
Update all related files

Commit to github
git add .
git commit -m “message”
git push origin main

Render
New project 
Web service
Copy paste public git repository 
Build command: npm install 
Start command: node server.js
Root directory: backend 

Environment variables 
MONGO_URI
PORT

Deploy

Copy link
Paste in frontend .env
API.js
Replace //localhost

Vercel
Log in
New project 
Git repo url

Node
Npm run build 
Dist
Npm install

check server.js 
route define
